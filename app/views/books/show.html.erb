<div class="show-comment">
  <p><strong>Title:</strong><%= @book.title %></p>
  <p><strong>impression:</strong><%= @book.impression %></p>
</div>

<table class="book-column">
  <thead>
    <th></th>
    <th></th>
  </thead>
  <tbody>
    <tr>
      <td class="border"><%= link_to "Edit", edit_book_path(@book.id) %></td>
      <td><%= link_to "Beak", books_path(@book.id) %></td>
    <tr>
  <tbody>
